# Leave blank when your API is served from the same domain as this frontend.
# For a separate local server, use for example: http://localhost:3000
VITE_API_URL=http://localhost:3000
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/jumpstart?schema=public
JWT_SECRET=replace-with-a-long-random-secret
CLIENT_URL=http://localhost:5173
PORT=3000
